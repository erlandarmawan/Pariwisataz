<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uusbotak.pariwisata">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".List_Wisata" />
        <activity android:name=".cipanas" />
        <activity android:name=".talaga_bodas" />
        <activity android:name=".sanghyang_taraje" />
        <activity android:name=".darajat" />
        <activity android:name=".tampil_wisata" />
        <activity android:name=".candi_cangkuang" />
        <activity android:name=".pantai_santolo" />
        <activity android:name=".gunung_papandayan"></activity>
    </application>

</manifest>